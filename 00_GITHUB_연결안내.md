# GitHub 연결 안내

이 폴더는 Codex·Claude 다중 계정 사용량 모니터의 공식 로컬 작업 폴더다.

- 공식 로컬 정본: `C:\VibeCoding\2. 깃헙\260917_AI-사용량모니터_개인작업`
- 외부 원본: `https://github.com/CodeZeno/Claude-Code-Usage-Monitor`
- 원격 이름: `origin`
- 사용자 기능 브랜치: `custom/four-account-monitor`
- 기준 원본 브랜치: `main`

현재 `origin`은 외부 오픈소스 원본이며, `jjhytbiz9-ux` 계정의 별도 저장소는 아직 만들지 않았다. 개인 원격을 새로 만들기 전에는 사용자 기능 브랜치를 `origin`에 임의로 푸시하지 않는다.

앞으로 이 모니터의 코드·테마·설치 스크립트는 위 로컬 정본에서 수정한다. `C:\Users\user\Downloads\AI-Usage-Monitor`를 다시 만들거나 중복 clone하지 않는다.

로그인 토큰과 계정 프로필은 저장소 밖의 `C:\Users\user\.ai-usage-accounts` 등에 보관하며 Git에 추가하지 않는다. 설치본과 사용자 설정은 Codex 패키지 가상화 바깥인 `%USERPROFILE%\Applications\AIUsageMonitor` 및 그 아래 `Data` 폴더에 둔다.
